[build]
  command = "npx nypm install && npm run build"
  publish = "dist"

[build.environment]
  NITRO_PRESET = "netlify"
  SECRETS_SCAN_OMIT_KEYS = "DEEPSEEK_API_KEY,TURSO_AUTH_TOKEN,R2_SECRET_ACCESS_KEY,R2_ACCESS_KEY_ID"
