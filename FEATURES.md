# yu reader 功能介绍

## 1. 文本导入与智能分段

支持多种内容来源导入：粘贴纯文本、上传 TXT/Markdown/PDF 文件、导入 YouTube 字幕、输入网页 URL（自动提取正文）。导入后 AI 自动分析文章结构并智能分段，用户可手动调整分段粒度（粗/中/细），也可对段落进行编辑、合并、拆分、删除和插入操作。

每篇文章会保存完整的阅读进度，下次打开自动定位到上次阅读位置。文章列表支持文件夹分类管理，可按文件夹筛选、搜索标题、拖拽移动文章到不同文件夹。

---

## 2. AI 智能分析

每篇文章导入后，AI 自动生成深度分析报告，包含：文章标题提炼、写作风格识别、内容摘要、背景知识介绍、以及全文关键要点提取。分析结果以结构化卡片展示在阅读页侧边面板中，帮助读者在深入阅读前建立整体认知框架。

---

## 3. 划词标记系统

阅读过程中选中文本即可弹出工具条，支持五种标记类型：

- **生词标记**（黄色）：AI 自动提取词元（lemma），生成音标、词性、基本释义、详细用法说明、语境分析和语域标注
- **短语标记**（绿色）：AI 解析固定搭配和习语，给出核心含义、用法说明和替代表达
- **好句标记**（青色）：AI 分析句子语法结构、修辞手法、在段落中的作用
- **知识要点标记**（粉色）：摘录重要观点和知识点到知识库
- **翻译**（紫色）：AI 翻译选中内容

所有标记自动同步到对应单词本（默认单词本 / 默认短语本 / 默认句子本）。标记在文中以高亮背景色显示，鼠标悬停可查看 AI 解析结果。删除标记时自动清理单词本中对应记录。

---

## 4. AI 段落对话

每个段落旁有 AI 对话按钮，点击可针对该段落内容提问。AI 会结合段落上下文和全文背景进行回答，支持流式输出打字动画。对话历史按段落独立保存，下次打开文章时可继续之前的对话。也支持全文级别的 AI 对话。

---

## 5. 单词本管理

系统内置三个默认单词本（单词、短语、句子），也支持创建自定义单词本。单词本中可查看每条单词的完整信息：词条、音标、释义、词性、笔记、来源文章、学习进度。支持按词性筛选、批量删除、导出为 Anki CSV 或 Markdown 格式。支持 AI 批量补全释义和词性。

单词由标记自动同步生成，lemma（原形）优先作为主词条，原始文本中的形式作为笔记保存。支持词性 AI 补充和去重整理。

---

## 6. 闪卡学习（SM-2 算法）

基于 SM-2 间隔重复算法实现科学记忆。每张卡片正面显示单词和音标，背面显示释义、词性和笔记。学习时用户评价记忆难度（重来/困难/良好/简单），算法自动计算下次复习时间。

交互上支持点击翻页、键盘快捷键（方向键/数字键）和拖拽翻页。学习数据（正确次数、错误次数、复习间隔、记忆难度系数）持久化存储在数据库中。

---

## 7. 复习本

集中展示所有文章中的标记内容，以卡片网格形式呈现。每条标记显示：原文、释义摘要、音标、词性、所属文章和文件夹。支持按标记类型（单词/短语/句子）和词性筛选。支持一键发音。可导出为 Anki CSV 或 Markdown 格式用于外部学习工具。

---

## 8. 每日学习

日历视图展示每日学习记录，点击具体日期进入每日详情页，包含：当日标记数统计、词性分布图、标记单词列表、以及 AI 生成的每日学习洞察报告（综合分析当日学习内容的规律和重点）。

每日统计直接读取 texts.marks 中的标记创建时间进行聚合，数据实时准确。

---

## 9. 词汇分析仪表盘

全局词汇分析包含：整体词性分布统计、高频重复词汇列表（识别哪些词在多篇文章中反复出现）、困难词汇标记（学习正确率低的词）、以及 AI 语义聚类分析（将含义相关的单词自动分组，发现词汇间的内在联系）。

---

## 10. 知识库

从阅读中提取的知识要点集中管理，每个要点保留来源文章和上下文信息。支持自定义分组整理，可拖拽调整排序。每个知识要点支持独立 AI 对话，可深入探讨该知识点。知识库也可从段落笔记自动导入。

---

## 11. 视频学习

支持导入 YouTube 视频字幕（自动去重），也可上传本地视频和字幕文件。播放器支持三种模式：

- **视频模式**：边看视频边对照字幕阅读
- **音频模式**：纯音频播放，适合听力练习
- **学习模式**：字幕按句切分为独立卡片，支持跟读练习

跟读练习记录每句的练习状态（未练习/练习中/已完成），练习数据持久化保存。

---

## 12. 多端数据同步

采用 sql.js（浏览器端 SQLite）+ Turso（云端 SQLite）双数据库架构。本地数据库运行在浏览器内存中，同时持久化到硬盘 JSON 文件做备份。云端 Turso 数据库支持多设备间的数据同步。

同步策略为增量同步：以 `updatedAt` 时间戳为依据，只推送/拉取有变更的行，避免全量传输。写入操作后 30 秒自动推送到 Turso，每 60 秒自动从 Turso 拉取远端更新。同步方向为"最新更新为准"，不会产生冲突。

---

## 13. 笔记系统

阅读时可为任意段落添加笔记，笔记以 Markdown 格式保存。段落有笔记时在侧边显示笔记图标，点击可查看和编辑。笔记数据存储在 texts 表的 paragraphNotes JSON 字段中，跟随文章同步。

---

## 14. 阅读体验优化

支持调整阅读区域的宽度（阅读 / 舒适 / 专注三档）、字体大小、行间距。支持深色模式切换。页面布局为三栏结构：左侧文件夹侧边栏（可折叠） + 中间文章列表 + 右侧内容区。移动端侧边栏默认折叠为 44px 窄工具条，点击展开为毛玻璃遮罩浮层。
